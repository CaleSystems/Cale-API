DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
SENTRY_DSN=https://<key>@o4511983606628352.ingest.de.sentry.io/<project-id>
# Distinct secrets so a leaked access-token secret can't forge a refresh
# token. Generate with: node -e "console.log(require('crypto').randomBytes(48).toString('base64'))"
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
